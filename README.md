# IT News Portal 🚀

Современный новостной портал, разработанный с использованием Next.js 15, Strapi CMS и Tailwind CSS.

## 📋 Описание

Полнофункциональный новостной портал с красивым дизайном и отличной архитектурой. Проект демонстрирует лучшие практики разработки на Next.js и интеграцию со Strapi CMS.

### Основные возможности

- ✨ **Главная страница** с отображением всех новостей в виде карточек
- 🔍 **Поиск по заголовкам** новостей в реальном времени
- 🏷️ **Фильтрация по тегам** с возможностью выбора нескольких тегов
- 📄 **Пагинация** для удобной навигации по новостям
- 📰 **Страница отдельной новости** с полным содержимым
- 🎨 **Современный UI/UX** дизайн с использованием Tailwind CSS
- 📱 **Адаптивная верстка** для всех устройств
- ⚡ **Высокая производительность** благодаря SSR и ISR Next.js
- 🔧 **TypeScript** для типобезопасности
- 🎯 **SEO-оптимизация** с динамическими метаданными

## 🏗️ Архитектура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout с метаданными
│   ├── page.tsx           # Главная страница
│   ├── globals.css        # Глобальные стили
│   └── news/
│       └── [id]/          # Динамический роут для новостей
│           ├── page.tsx   # Страница новости
│           └── not-found.tsx # 404 страница
├── components/            # Переиспользуемые компоненты
│   ├── Header.tsx         # Шапка сайта
│   ├── NewsCard.tsx       # Карточка новости
│   ├── SearchBar.tsx      # Строка поиска
│   ├── FilterSidebar.tsx  # Боковая панель фильтров
│   ├── Pagination.tsx     # Компонент пагинации
│   └── RichTextRenderer.tsx # Рендер rich-text контента
├── lib/                   # Утилиты и хелперы
│   └── strapi.ts          # API функции для Strapi
└── types/                 # TypeScript типы
    └── news.ts            # Типы для новостей
```

### Применённые практики

1. **Server Components по умолчанию** - используются серверные компоненты Next.js для лучшей производительности
2. **Client Components только где необходимо** - интерактивные компоненты помечены 'use client'
3. **Separation of Concerns** - чёткое разделение логики на компоненты, утилиты и типы
4. **Type Safety** - полная типизация с TypeScript
5. **Error Handling** - обработка ошибок с fallback UI
6. **Loading States** - skeleton screens для улучшения UX
7. **Responsive Design** - адаптивный дизайн mobile-first
8. **Semantic HTML** - использование семантических тегов для доступности
9. **SEO Optimization** - динамические метаданные для каждой страницы

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- npm или yarn
- Docker и Docker Compose (для Strapi backend)

### Установка и запуск

1. **Клонируйте репозиторий**

```bash
git clone <repository-url>
cd next-strapi-it-news
```

2. **Установите зависимости**

```bash
npm install
```

3. **Создайте файл переменных окружения**

```bash
# Создайте .env.local в корне проекта
echo "NEXT_PUBLIC_STRAPI_API_URL=http://localhost:1337" > .env.local
```

4. **Запустите Strapi backend**

```bash
# Запустите Docker контейнеры (PostgreSQL + MinIO + Strapi)
docker-compose up -d

# Или запустите Strapi отдельно
npm run strapi:dev
```

5. **Запустите Next.js приложение**

```bash
npm run dev
```

6. **Откройте браузер**

- Frontend: [http://localhost:3000](http://localhost:3000)
- Strapi Admin: [http://localhost:1337/admin](http://localhost:1337/admin)

## 📝 Настройка Strapi

### Коллекция News Article

В Strapi уже настроена коллекция `news-article` со следующими полями:

- **title** (Text) - Заголовок новости
- **content** (Rich Text) - Полное содержание новости с форматированием
- **tags** (JSON) - Массив тегов для категоризации
- **preview** (Media) - Превью изображение новости

### API Endpoints

- `GET /api/news-articles` - Получить все новости с фильтрами
- `GET /api/news-articles/:id` - Получить конкретную новость

### Права доступа

Убедитесь, что в Strapi настроены публичные права для:

- `find` - просмотр всех новостей
- `findOne` - просмотр одной новости

## 🎨 Технологии

- **[Next.js 15](https://nextjs.org/)** - React фреймворк с App Router
- **[React 19](https://react.dev/)** - Библиотека для создания UI
- **[TypeScript](https://www.typescriptlang.org/)** - Типизированный JavaScript
- **[Tailwind CSS 4](https://tailwindcss.com/)** - Utility-first CSS фреймворк
- **[Strapi 5](https://strapi.io/)** - Headless CMS
- **[Docker](https://www.docker.com/)** - Контейнеризация

## 📚 Основные компоненты

### Header

Красивая шапка сайта с градиентом, логотипом и навигацией. Включает декоративные элементы и адаптивный дизайн.

### NewsCard

Карточка новости с:

- Превью изображением с hover эффектом
- Заголовком и кратким описанием
- Тегами новости
- Датой публикации
- Плавной анимацией при наведении

### SearchBar

Компонент поиска с:

- Дебаунсом для оптимизации запросов
- Кнопкой очистки
- Интеграцией с URL параметрами

### FilterSidebar

Боковая панель фильтрации с:

- Чекбоксами для выбора тегов
- Счётчиком выбранных фильтров
- Кнопкой сброса фильтров

### Pagination

Продвинутая пагинация с:

- Умной навигацией по страницам
- Отображением текущей страницы
- Кнопками prev/next
- Информацией о количестве элементов

### RichTextRenderer

Компонент для рендеринга rich-text контента из Strapi с красивой типографикой.

## 🔧 Скрипты

```bash
# Разработка
npm run dev              # Запуск Next.js в dev режиме
npm run strapi:dev       # Запуск Strapi в dev режиме

# Production
npm run build            # Сборка проекта
npm run start            # Запуск production сервера

# Линтинг
npm run lint             # Проверка кода ESLint
```

## 🌐 API интеграция

### Fetching данных

Проект использует серверные компоненты Next.js для fetching данных:

```typescript
// Получение всех новостей с фильтрами
const newsResponse = await getNewsArticles({
  search: "технологии",
  tags: ["AI", "Web"],
  page: 1,
  pageSize: 9,
});

// Получение одной новости
const article = await getNewsArticleById("article-id");
```

### Кеширование

- Используется встроенное кеширование Next.js
- `revalidate: 60` - перевалидация каждые 60 секунд
- ISR (Incremental Static Regeneration) для оптимальной производительности

## 📱 Адаптивность

Проект полностью адаптивен для всех устройств:

- **Mobile** (< 768px) - 1 колонка карточек
- **Tablet** (768px - 1024px) - 2 колонки карточек
- **Desktop** (> 1024px) - 3 колонки карточек + боковая панель

## 🎯 SEO

- Динамические метаданные для каждой страницы
- Open Graph теги для социальных сетей
- Semantic HTML разметка
- Оптимизация изображений с Next.js Image
- Structured data готов к добавлению

## 🤝 Вклад

Проект создан для демонстрации лучших практик разработки на Next.js и Strapi.

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Разработано с ❤️ для демонстрации современного стека веб-разработки.

---

**Приятного использования! 🎉**
